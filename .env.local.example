# Optional — set this when you wire up the contact form to a Google Sheet.
# Create an Apps Script Web App on a sheet (Extensions → Apps Script → Deploy as Web App)
# and paste its URL here. Until then, the form runs in preview mode and
# logs submissions to the Vercel function logs.
SHEET_WEBHOOK_URL=
